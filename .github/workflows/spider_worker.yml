name: 🕷️ Spider Bot (Every 2h)

on:
  schedule:
    # 每 2 小时运行一次 (0:00, 2:00, 4:00...)
    - cron: '0 */2 * * *'
  workflow_dispatch: # 允许手动点按钮触发

permissions:
  contents: write

jobs:
  spider-job:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 下载代码
        uses: actions/checkout@v3

      - name: 🐍 设置 Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 📦 安装依赖
        run: pip install requests

      - name: 🕷️ 运行爬虫
        run: python main.py

      - name: 💾 保存数据
        run: |
          git config --global user.name 'Spider Bot'
          git config --global user.email 'spider@bot.com'
          git add data_*.json
          # 只有当有新数据时才提交，避免报错
          git commit -m "🕷️ Auto-update: Raw Data" || exit 0
          git push
