name: 🕷️ Spider Worker (Every 30 mins)

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  spider-job:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 下载代码
        uses: actions/checkout@v3

      - name: 🐍 设置 Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 📦 安装依赖
        run: pip install requests

      - name: 🕷️ 运行爬虫
        run: python crab.py

      - name: 💾 保存数据
        run: |
          git config --global user.name 'Spider Bot'
          git config --global user.email 'spider@bot.com'
          # 添加 data_*.json 以及 archives 和 history 目录
          git add data_*.json archives/ history/
          git commit -m "🕷️ Auto-update: Raw Data & Archives" || exit 0
          
          # 防止并发冲突
          git pull --rebase origin main
          
          git push